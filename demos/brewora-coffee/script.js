const reduced=matchMedia('(prefers-reduced-motion: reduce)').matches;if(!reduced){const zooms=[...document.querySelectorAll('.zoom img')];const tick=()=>{const h=innerHeight;zooms.forEach(img=>{const r=img.parentElement.getBoundingClientRect();if(r.bottom>0&&r.top<h){const p=Math.max(0,Math.min(1,(h-r.top)/(h+r.height)));img.style.transform=`scale(${1.04+p*.045}) translateY(${(p-.5)*-7}px)`}else img.style.transform='scale(1.04)'});requestAnimationFrame(tick)};requestAnimationFrame(tick)}const header=document.querySelector('header');addEventListener('scroll',()=>{header.style.background=scrollY>50?'rgba(8,8,8,.94)':'linear-gradient(#080808cc,transparent)'},{passive:true});